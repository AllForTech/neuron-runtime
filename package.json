{
  "name": "neuron-runtime",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build:shared": "npm run build -w @neuron/shared",
    "build:backend": "npm run build -w backend",
    "build": "npm run build:shared && npm run build:backend",
    "dev:backend": "npm run dev -w backend",
    "dev:frontend": "npm run dev -w frontend"
  },
  "license": "ISC"
}